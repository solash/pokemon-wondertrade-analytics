$('#users-chart').highcharts({
    title: {
        text: 'Wonder Trades by Contributers'
    },
    color: ['#fff00', '#000fff'],
    tooltip: {
        pointFormat: '<b>{point.y}</b>'
    },
    plotOptions: {
        pie: {
            allowPointSelect: true,
            cursor: 'pointer',
            dataLabels: {
                enabled: true,
                color: '#000000',
                connectorColor: '#000000',
                format: '<b>{point.name}</b>: {point.percentage:.1f} %'
            }
        }
    },
    series: [{
        type: 'pie',
        name: 'Country',
        data: <%- userChart %>
    }]
});